# Antigravity 비개발자 매뉴얼 완성도 재평가

평가 대상: `antigravity-cli-inventory-vibe-coding-manual.html`

## 종합 평가

**현재 완성도: 5/10**

이전보다 목차, 번호, 운영체제 구분, 용어 설명과 최종 체크리스트가 개선됐습니다. 그러나 기술적으로 잘못된 설치 안내와 위험한 데이터베이스 보안 정책이 남아 있어, 비개발자가 그대로 따라 하는 공식 매뉴얼로 공개하기에는 아직 부족합니다.

| 평가 항목 | 점수 | 평가 |
|---|---:|---|
| 시각 디자인 | 8/10 | 일관되고 보기 좋음 |
| 정보 구조 | 7/10 | 1~16 순서와 OS 구분 개선 |
| 비개발자 친화성 | 5/10 | 용어집은 좋지만 중간 절차가 부족함 |
| 기술 정확성 | 3/10 | 설치·인증·CORS 설명 수정 필요 |
| 보안 | 2/10 | 익명 전체 CRUD 허용 정책이 위험함 |
| 실제 재현성 | 4/10 | 실행 위치와 성공 기준이 부족함 |

## 좋아진 부분

- 목차 번호가 1~16 순서로 정리됐습니다.
- macOS와 Windows 명령을 구분했습니다.
- PAT, Supabase Key, `.env` 용어 설명을 추가했습니다.
- 복사 가능한 SQL과 최종 점검 체크리스트가 생겼습니다.
- 작은 화면에서 목차가 사라지던 문제가 개선됐습니다.

## 반드시 수정할 사항

### 1. Antigravity 설치와 로그인을 공식 절차로 변경

현재 문서의 다음 명령은 공식 설치 절차와 다릅니다.

```bash
npm install -g agy
agy login
```

macOS/Linux 권장 설치:

```bash
curl -fsSL https://antigravity.google/cli/install.sh | bash
agy
```

Windows 권장 설치:

```powershell
irm https://antigravity.google/cli/install.ps1 | iex
agy
```

첫 실행 시 Google OAuth, 인증 코드, 약관 동의, 작업 폴더 신뢰 설정 과정을 설명해야 합니다. `1초 로그인` 표현은 제거합니다.

### 2. 예상 시간을 현실적인 범위로 변경

- 계정과 프로그램이 준비된 경우: 20~40분
- 완전한 첫 시도: 45~90분
- 설치 또는 인증 오류 발생 시: 1~2시간

실제 측정 기록이 없다면 `검증 시간`이 아니라 `예상 시간`이라고 표시합니다.

### 3. 익명 전체 CRUD 정책 제거

현재 SQL은 인증하지 않은 사람도 제품을 추가·수정·삭제할 수 있습니다.

권장 권한:

- 비로그인 사용자: 읽기만 허용
- 로그인 사용자: 추가·수정 허용
- 관리자: 삭제 허용
- 사용자 또는 회사별 데이터 분리

RLS는 활성화 여부보다 정책 내용이 안전한지가 중요합니다.

### 4. Supabase 키 안내 갱신

2026년 기준 권장 예시:

```env
VITE_SUPABASE_URL=프로젝트_URL
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
```

- Publishable Key: 현재 권장하는 브라우저용 키
- Anon Key: 기존 프로젝트에서 사용하는 Legacy 키
- Secret/Service Role Key: 브라우저 사용 금지

### 5. MCP 연결 방법 추가

현재 MCP 절에는 자연어 예시만 있고 실제 연결 방법이 없습니다.

다음 내용을 추가해야 합니다.

- MCP 연결과 OAuth 로그인 방법
- 대상 Supabase 프로젝트 지정
- 읽기 전용 설정
- 연결 성공 확인 방법
- 수정·삭제 전 사용자 승인 규칙

처음에는 프로젝트 범위를 제한하고 `read_only=true` 사용을 권장합니다.

### 6. GitHub 5대 원칙 완성

현재 제목은 `5대 원칙`이지만 실제 내용은 2개입니다. 다음 항목을 추가합니다.

1. 비밀 정보 유출 방지
2. Conventional Commits 사용
3. 기능별 브랜치 사용
4. Pull Request로 변경 내용 확인
5. 배포 전 빌드와 테스트 실행

### 7. `_redirects` 설명 수정

현재 설명의 `CORS / 404 방지`는 잘못됐습니다. `_redirects`는 CORS를 해결하지 않습니다.

올바른 설명:

```text
React Router 서브페이지를 새로고침할 때 발생하는 404 방지
```

## 비개발자용 작성 형식

각 단계를 다음 형식으로 통일합니다.

1. 이번 단계에서 할 일
2. 실행 위치
3. 클릭할 메뉴 또는 복사할 명령
4. 정상 완료 화면
5. 오류가 발생했을 때 해결 방법

예시:

```text
실행 위치: vibe-inventory 프로젝트 폴더
성공 기준: 버전 번호 또는 완료 메시지가 표시됩니다.
문제가 생기면: 아래 오류 해결 항목을 확인하세요.
```

환경 설치 후 확인 명령:

```bash
node --version
npm --version
git --version
gh --version
agy --version
```

## 줄여야 할 표현

- `100% 활용`
- `마스터 종합 백과사전`
- `공식 인증 매뉴얼`
- `완벽 분리 지원`
- `완전 무결`
- `1초 로그인`
- `자가 치유`

추천 제목:

> Antigravity CLI로 재고관리 앱 만들기  
> 비개발자를 위한 GitHub·Supabase·Cloudflare 실습 가이드

## 공개 가능 여부

| 사용 목적 | 판단 |
|---|---|
| 개인 발표·시연 | 사용 가능 |
| 내부 학습 | 주의 문구 추가 후 가능 |
| 비개발자용 따라 하기 매뉴얼 | 핵심 오류 수정 후 가능 |
| 실제 업무 시스템 구축 지침 | 현재 상태로 사용 불가 |

Antigravity 설치, Supabase RLS, 키 체계, MCP 연결, CORS 설명을 수정하면 약 **7/10**, 단계별 성공 화면과 오류 해결까지 추가하면 비개발자용 매뉴얼로 공개 가능한 **8/10 수준**까지 개선할 수 있습니다.

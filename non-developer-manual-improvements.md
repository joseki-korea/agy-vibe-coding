# 비개발자용 매뉴얼 개선 사항

## 개선 방향

비개발자가 그대로 따라 할 수 있도록 모든 단계를 아래 형식으로 통일합니다.

1. 이번 단계에서 할 일
2. 클릭할 위치 또는 실행할 명령어
3. 정상적으로 완료된 화면
4. 오류가 발생했을 때 해결 방법

## 핵심 개선 사항

- `100%`, `1초 인증`, `완전 무결`, `마스터` 같은 과장 표현을 제거합니다.
- 예상 시간은 `준비 완료 시 20~30분`, `첫 시도 40~60분`처럼 범위로 안내합니다.
- 시작 전에 Google, GitHub, Supabase, Cloudflare 계정과 Node.js, Git 등 준비물을 확인합니다.
- macOS, Windows, Linux 명령어를 구분해서 제공합니다.
- CLI, DB, RLS, MCP, 배포 같은 용어는 처음 등장할 때 쉬운 말로 설명합니다.
- 모든 명령어에 실행할 폴더와 성공 확인 방법을 함께 표시합니다.
- Antigravity CLI 설치와 로그인은 최신 공식 절차로 수정합니다.
- Supabase는 Publishable Key를 사용하고 Secret Key는 브라우저에 넣지 않도록 경고합니다.
- 익명 전체 읽기·쓰기 정책을 제거하고 로그인 사용자 중심의 RLS 정책을 사용합니다.
- MCP는 연결 방법, 프로젝트 제한, 읽기 전용 설정과 권한 위험을 설명합니다.
- 긴 올인원 프롬프트 대신 화면 생성 → DB 연결 → 배포의 3단계로 나눕니다.
- 각 단계가 끝날 때 실제 기능이 작동하는지 체크리스트로 확인합니다.
- 모바일 목차, 모든 코드의 복사 버튼, 번호가 표시된 스크린샷을 제공합니다.

## 권장 진행 순서

1. 완성 결과 미리 보기
2. 예상 시간과 주의사항
3. 필요한 계정과 프로그램 확인
4. 운영체제별 Antigravity CLI 설치
5. 작업 폴더 생성
6. 재고관리 화면 만들기
7. 로컬 실행 및 기능 확인
8. Supabase 프로젝트와 테이블 만들기
9. 안전한 RLS 설정
10. 앱과 Supabase 연결
11. GitHub 저장소에 코드 보관
12. Cloudflare Pages 배포
13. 최종 기능 점검
14. 자주 발생하는 오류 해결

## 단계 작성 예시

### Antigravity CLI 실행하기

이번 단계에서는 Antigravity CLI를 실행하고 Google 계정으로 로그인합니다.

터미널에서 다음 명령을 실행합니다.

```bash
agy
```

Google OAuth를 선택하고 브라우저 로그인을 완료합니다.

**성공 기준:** Antigravity에 질문을 입력할 수 있는 화면이 나타납니다.

**오류 해결:** `command not found: agy`가 나오면 설치 또는 PATH 설정을 다시 확인합니다.

## 최종 확인 체크리스트

- [ ] 재고 목록과 검색 기능이 작동한다.
- [ ] 제품 추가와 입출고 결과가 Supabase에 저장된다.
- [ ] 새로고침해도 데이터가 유지된다.
- [ ] RLS가 활성화되어 있고 익명 사용자가 데이터를 변경할 수 없다.
- [ ] `.env.local`과 Secret Key가 GitHub에 올라가지 않았다.
- [ ] Cloudflare 배포 주소가 PC와 휴대전화에서 열린다.
- [ ] 서브페이지를 새로고침해도 404 오류가 발생하지 않는다.

> 이 프로젝트는 학습용 예제입니다. 실제 고객 정보나 회사 재고를 사용하기 전에는 별도의 보안 검토가 필요합니다.
